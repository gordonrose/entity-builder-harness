# Chat Script Aliases

This directory exposes chat-layer script entrypoints.

The implementations still live under `scripts/shared/` for compatibility with
active sessions and existing tooling. Keep these wrappers thin until the
migration plan explicitly moves executable ownership.

