# agentic-artifact:
#   schema: agentic-artifact/v2
#   id: rag-rulebook.evaluation.retrieval-selector.v1.packages-core-queues-contract
#   version: 1
#   status: active
#   layer: 02.rag-rulebook
#   domain: retrieval
#   disciplines:
#   - agentic
#   - architecture
#   kind: evaluation-fixture
#   purpose: Prove packages/core queue prompts retrieve queue contract and worker/provider-boundary guidance.
#   portability:
#     class: reusable
#     targets:
#     - llm-workbench
#     - entity-builder
#     - design-system-builder
#   used_by:
#   - id: rag-rulebook.script.evaluate-retrieval-selector-fixtures
#     path: scripts/02.rag-rulebook/evaluate-retrieval-selector-fixtures/script.sh

schema: rag-rulebook/retrieval-selector-evaluation/v1
fixture_id: retrieval-selector.v1.packages-core-queues-contract
status: active
owner_layer: 02.rag-rulebook
purpose: Prove prompts about packages/core queue contracts retrieve queue message, message-version, payload, retry/dead-letter, worker, adapter, and infra boundary rules.
input:
  request_text: Explain how product code should use packages/core queue contracts for queue messages, schema versions, JSON-safe payloads, send options, delivery metadata, handlers, retry and dead-letter facts, and idempotency.
  session:
    trust_routing: true
    layer: 03.product
    mode: discovery
    workflow: .agentic/product/workflows/default.md
  focused_paths:
    - packages/core/src/queues/index.ts
    - docs/harness/architecture/rules/layers/packages-core.yml
    - docs/harness/architecture/rules/concerns/events-messaging-async.yml
  max_chunks: 10
expected:
  routing:
    layer: 03.product
    status: ready
  matched_corpora:
    required:
      - corpus.03.product.core
  selected_corpora:
    required:
      - corpus.03.product.core
  selected_content_kinds:
    required:
      - rule
  selected_chunks:
    required_rule_ids:
      - core.queues-contracts-name-retryable-work
      - core.capability-modules-stay-contract-shaped
      - async.core-contracts-platform-runtime-app-meaning
      - capability-layering.queues-follow-the-same-split
  required_checks:
    min_count: 1
  citations:
    min_count: 1
  gaps:
    no_unexpected: true
banned:
  selected_corpora:
    - corpus.00.chat
source_refs:
  - docs/harness/architecture/source-material/packages-core-contract-surface-v1.md
  - docs/harness/architecture/rules/layers/packages-core.yml
  - docs/harness/architecture/rules/concerns/events-messaging-async.yml
  - docs/harness/architecture/rules/concerns/platform-infra-capability-layering.yml
  - .agentic/02.rag-rulebook/policies/retrieval-selector/v1.yml
update_triggers:
  - packages/core queue ownership rules change.
  - Async or capability-layering rules change queue ownership boundaries.
  - Selector policy changes corpus ownership or evidence bundle behavior.
