# agentic-artifact:
#   schema: agentic-artifact/v2
#   id: rag-rulebook.evaluation.retrieval-selector.v1.container-image-management
#   version: 1
#   status: active
#   layer: 02.rag-rulebook
#   domain: retrieval
#   disciplines:
#     - agentic
#     - architecture
#     - sre
#     - security
#   kind: evaluation-fixture
#   purpose: Prove container-governance prompts retrieve the shared deploy container-image rule instead of only target-specific ECS rules.
#   portability:
#     class: reusable
#     targets:
#       - llm-workbench
#       - entity-builder
#       - design-system-builder
#   used_by:
#     - id: rag-rulebook.script.evaluate-retrieval-selector-fixtures
#       path: scripts/02.rag-rulebook/evaluate-retrieval-selector-fixtures/script.sh

schema: rag-rulebook/retrieval-selector-evaluation/v1
fixture_id: retrieval-selector.v1.container-image-management
status: active
owner_layer: 02.rag-rulebook
purpose: Prove questions about container sprawl and Dockerfile placement retrieve shared deploy container governance.
input:
  request_text: How should we manage Dockerfiles and container images in this repo so image definitions do not sprawl?
  session:
    trust_routing: true
    layer: 04.deploy
    mode: planning
    workflow: .agentic/aws/workflows/plan-aws-change.md
  max_chunks: 10
expected:
  routing:
    layer: 04.deploy
    mode: planning
    workflow: .agentic/aws/workflows/plan-aws-change.md
    status: ready
  intent:
    id: intent.planning.decision-support
  action_authorization:
    requested_action: context-retrieval
    side_effect_class: none
    execution_allowed: false
    status: not-requested
    resolved_intent_id: intent.planning.decision-support
  matched_corpora:
    required:
      - corpus.04.deploy
  selected_corpora:
    required:
      - corpus.04.deploy
  selected_content_kinds:
    required:
      - rule
  selected_chunks:
    required_artifact_ids:
      - artifact.concern.container.image.management
    required_rule_ids:
      - container-image-management.uses-infra-image-boundary
      - container-image-management.bounds-build-context
  recognition:
    required_source_ids:
      - recognition.generated.routing
      - recognition.curated.question-categories
    required_canonical_ids:
      - 04.deploy
      - evidence.deploy.container-image-management
    required_matched_inputs:
      - prompt
      - session-metadata
    required_matches:
      - source_id: recognition.curated.question-categories
        term: container images
        category: evidence-family
        canonical_id: evidence.deploy.container-image-management
        matched_input: prompt
  gaps:
    allowed:
      - gap.selector-fixture.prompt-session-layer-conflict
    no_unexpected: true
  required_checks:
    min_count: 1
  citations:
    min_count: 1
banned:
  action_authorization:
    execution_allowed: true
  selected_corpora:
    - corpus.03.product
source_refs:
  - docs/04.deploy/source-material/shared/container-image-management.md
  - docs/04.deploy/rules/shared/container-image-management.yml
  - scripts/04.deploy/validate-container-boundaries/script.sh
update_triggers:
  - Container image management rule changes.
  - Container boundary validator behavior changes.
  - Selector ranking, deploy planning intent, or shared deploy corpus behavior changes.
