id: concern.example
title: Example concern rules
version: 1
status: active

description: >
  ...

applies_to:
  paths:
    - "apps/**"
    - "packages/**"
    - "platform/**"

rules:
  - id: concern.example-rule
    title: Example concern rule
    severity: error
    summary: >
      ...
    must:
      - "..."
    must_not:
      - "..."
    agent_guidance: >
      ...

source_refs:
  - doc: "example.md"
    sections:
      - "Example section"
